# 市场消费者接入进展

本分支基于 Bridge 0.6.21 开发，用户已确认目标版本 0.7.0，尚未发布。本文件记录开发状态，不是上线验收结论。

## 当前实现

- 桌面市场目录、精确版本与制品改为访问当前消费环境的 local-app-service。服务 context 返回权威 marketKey；来源 environmentKey 保留市场冻结合同中的原值。
- 当前设备工作区 PAT 只发送到消费环境；凭证必须显式绑定该环境 API 地址。新增绑定记录由设备授权写入；缺少绑定的历史凭证要求重新授权，不猜测所属环境。
- 市场卡片以 marketKey/listingId 区分，展示来源环境。安装请求传递完整 InstallSource；升级校验 marketKey/listingId/environmentKey/appId，精确版本包含 build metadata。
- Connector 安装与托管 CLI 安装使用环境服务返回的冻结制品；清单里的 source URL 不作为下载入口。Connector 验证安装包完整清单与审核快照一致。
- Connector 安装记录和摘要、托管 CLI 状态保存 InstallSource。CLI 回滚切换对应来源记录；来源不明的历史记录不会自动匹配市场升级。
- 内置 CLI 的来源由发布预检从消费环境核实并随二进制携带，导入、升级和回滚保留来源记录；缺失来源的旧制品保持未证明状态。发布构建必须具备匹配版本的来源文件。
- 平台制品优先匹配精确架构，其次匹配同平台的 universal 制品；下载和归档路径使用同一个已选制品，支持市场已有的 macOS universal CLI。
- CModel 生产依赖已统一到 5.0.0。共享 local-app-contract 已锁定已发布 1.1.0 的不可变提交 063df86d0e57920b712541734dd2cfd5f83e539b，正式依赖不使用本地 Cargo patch。

## 尚未完成的验收与发布工作

1. 桌面 Rust 单测已通过；实际安装/升级/回滚与授权联调仍需完成。
2. 历史安装来源的权威映射与保留数据的正式迁移；无证据时不得认领来源。
3. 本地数据、进程和设备注册仍以 appId 为键，不能声明支持同名应用跨环境同时安装；当前替换校验拒绝跨来源覆盖。
4. 发布流水线的旧匿名市场检查已切换到消费环境服务，部署前仍需登记验证身份及冻结来源。local-app-market 保留独立的旧只读目录；旧记录不会自动转成新来源身份，仍需明确支持窗口和迁移事实。
5. 最终版本、不可变依赖、发布顺序、多平台签名和线上验证。MINOR/MAJOR 版本须取得用户针对准确目标的单独确认，历史版本授权不适用。

## 已完成检查

- 前端 145 项测试、TypeScript 检查、桌面 Rust 106 项测试、桌面 Clippy 严格检查通过。
- 核心 169 项测试及 16 项消费者测试通过；新增 2 项安装来源隔离测试分别通过。
- 完整发布架构门禁、多平台打包签名及真实客户安装验收尚未完成，不能以这些开发检查代替。
- 本分支没有更改线上配置、生成版本标签或发布客户端制品。

## 0.7.0 发布准备（2026-09-10）

- 内置 CLI 从 0.49.0 更新至0.54.0。三个 ZIP 的 GitHub 附件、附带 SHA-256 和公共下载内容逐项一致，已更新 VERSION/SHA256SUMS。CLI GitHub 发行仍是 draft，需通过原发行工作流完成市场登记与收尾；不能把草稿当完整发行成功。
- 修正消费地址构造：平台 base URL 的 `/lowcode3` 协议后缀不属于 Partner API 基址；保留环境路径前缀，新增针对根地址及带前缀地址的测试。
- 完整 npm quality 通过：前端145、核心171、目录合同16、桌面106、迁移12；补充地址修正后的三个消费配置测试通过。浏览器启动/恢复4项通过。
- 线上消费环境正确 Partner 路径的 context 和 listings 仍返回404。注册 Jenkins 最近成功 local-app-service #9 对应1.0.6；本次新增读取门面在1.1.0，需要独立的平台组件发布和环境安装验证。不能用中央市场公开 listings 的成功代替环境服务验收。
- 发布工作流使用 BAIJIMU_CLI_MARKET_SOURCE 固定来源配置，离线校验后生成 INSTALL_SOURCE.json；环境消费检查独立进行。
- 不新建内网负载均衡。统一Token基础服务链路按已讨论的现有市场网关方案另行完成；客户端版本尚未生成标签或触发发行。

## 0.7.0 发布校验调整

客户端发布只读取 `BAIJIMU_CLI_MARKET_SOURCE`，离线校验固定 appId、版本和来源字段并生成 INSTALL_SOURCE.json；各平台继续按固定 SHA-256 校验 CLI 制品。发布不依赖环境在线或工作区 PAT。`verify-consumer-market.mjs` 保留为独立联调工具，不由发布流水线调用。环境消费链路已验证 CLI 0.54.0 的来源与三平台制品。
