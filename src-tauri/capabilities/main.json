{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local",
  "description": "Permissions for the trusted Bridge Agent main webview only.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-main-commands"]
}
