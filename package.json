{
  "name": "bridge-agent-desktop",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 1420",
    "build": "npm run build:web",
    "build:web": "tsc && vite build",
    "test": "vitest run",
    "test:browser": "playwright test",
    "test:native": "node scripts/test-native-frontend.mjs",
    "quality": "npm test && npm run quality:rust",
    "quality:rust:architecture": "node scripts/check-rust-architecture.mjs",
    "quality:rust": "npm run build && cargo fmt --all -- --check && cargo fmt --manifest-path src-tauri/Cargo.toml -- --check && cargo fmt --manifest-path tools/windows-uninstaller/Cargo.toml -- --check && cargo fmt --manifest-path migration-artifacts/unified-app-id/Cargo.toml -- --check && npm run quality:rust:architecture && cargo test --workspace && cargo test --manifest-path src-tauri/Cargo.toml && cargo test --locked --manifest-path migration-artifacts/unified-app-id/Cargo.toml && cargo clippy --workspace --all-targets -- -D warnings && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings && cargo clippy --locked --manifest-path migration-artifacts/unified-app-id/Cargo.toml --all-targets -- -D warnings && cargo check --release --manifest-path src-tauri/Cargo.toml",
    "build:tauri": "node scripts/prepare-tauri-build.mjs",
    "prepare:windows-uninstaller:quality": "node scripts/prepare-windows-uninstaller.mjs --unsigned-for-quality-gate --with-frontend",
    "preview": "vite preview --host 0.0.0.0 --port 1421",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:build:macos-universal": "tauri build --target universal-apple-darwin"
  },
  "dependencies": {
    "@tauri-apps/api": "2.10.1",
    "@tauri-apps/plugin-deep-link": "2.4.9",
    "@tauri-apps/plugin-log": "2.9.0",
    "lucide-react": "^1.26.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@tauri-apps/cli": "2.10.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.1",
    "jsdom": "^24.1.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.9"
  }
}
